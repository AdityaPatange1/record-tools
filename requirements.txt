# Audio I/O and processing
numpy>=1.24.0
soundfile>=0.12.1
pedalboard>=0.9.0
pyloudnorm>=0.1.1

# YouTube download (requires ffmpeg installed on the system)
yt-dlp>=2024.8.0

# Vocal / stem separation (instrumental = no_vocals stem; requires ffmpeg)
# PyTorch: install the wheel that matches your platform from https://pytorch.org
# or use: pip install torch torchaudio
demucs
torch
torchaudio
torchcodec
